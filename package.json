{
  "name": "calculator-project",
  "version": "1.0.0",
  "description": "calculator-project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass CSS/superstyle.scss CSS/style.css",
    "dev-server": "live-server src",
    "dev-sass": "sass CSS/superstyle.scss CSS/style.css --watch",
    "dev": "npm run dev-server & npm run dev-sass",
    "start": "echo \"No start script\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1",
    "node-sass": "^8.0.0"
  }
}
