{
  "name": "calculator-project",
  "version": "1.0.0",
  "description": "calculator-project",
  "main": "index.js",
  "dependencies": {
    "serve": "^14.0.0"
  }
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass STYLE/superstyle.scss STYLE/style.css",
    "build-ts": "tsc TS/calculator.ts TS/application.ts ",
    "dev-server": "live-server src",
    "dev-sass": "sass STYLE/superstyle.scss STYLE/style.css --watch",
    "dev": "npm run dev-server & npm run dev-sass",
    "start": "echo \"No start script\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1",
    "node-sass": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^4.9.3"
  }
}
